#include <stdio.h>
int main(){
	int x,y,z;
		printf("Enter number:");
		scanf("%d",&y);
				for(x=0;x < y;x++){
					printf("\n");
				for(z=0;z < y;z++)
					printf("*");
			}
	return 0;		
}
